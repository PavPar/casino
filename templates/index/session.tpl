<div class="session">
                    <p class="session__name">{NAME}</p>
                    <p class="session__info">{INFO}</p>
                    <p class="session__players">{CURRPLAYERS}/{MAXPLAYERS}</p>
                    <button name="session" class="session__button session__button_type-join" value="{SESSION_ID}">Подключиться</button>
</div>